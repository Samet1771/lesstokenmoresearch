# Keep LF in the repository so Linux and macOS clones are not full of ^M.
* text=auto eol=lf

# Windows-only scripts stay CRLF in the working tree.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
